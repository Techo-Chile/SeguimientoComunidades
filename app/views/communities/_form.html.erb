<%= simple_form_for @community do |f| %>
  <%= f.input :name, label:'Nombre'%>
  <%= f.input :location_id  , :collection => Location.all, label:'Localidad' %>
  <%= f.input :status_id, label:'Estatus'%>
  <%= f.button :submit %>
  <% end %>
